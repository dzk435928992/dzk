pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'Docker --version'
            }
        }
        stage('Test') { 
            steps {
                 sh 'Docker --version'
            }
        }
        
    }
}
