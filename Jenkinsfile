pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
        stage('Test') { 
            steps {
                 sh 'node --version'
            }
        }
        
    }
}
